%% Driver script for simulating the ODE function modelDriverOdeFun
% This script was autogenerated with OneModel v0.5.3 a74c00a   -   Fernando Nóbel (fersann1@upv.es).

clear all;
close all;

[p,x0,M] = modelDefaultParam();

% Options for the solver.
opt = odeset('AbsTol',1e-8,'RelTol',1e-8);
opt = odeset(opt,'Mass',M);

% Simulation time span.
tspan = [0 10];

[t,x] = ode15s(@(t,x) modelOdeFun(t,x,p), tspan, x0, opt);
[out] = modelState(t,x,p);

plot(t,x);
legend('x1','x2','x3');
grid on;
